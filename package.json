{
  "name": "spotted",
  "version": "1.0.0",
  "description": "Backend API dla formularza Spotted",
  "main": "app.js",
  "scripts": {
    "dev": "nodemon --exec tsx src/server.ts",
    "build": "rimraf dist && tsc",
    "start": "node app.js",
    "deploy": "npm run build && tar -czf deploy.tar.gz app.js dist package.json package-lock.json && scp deploy.tar.gz contdev@s56.mydevil.net:~/domains/api.spottedlezajsk.pl/public_nodejs/ && ssh contdev@s56.mydevil.net \"cd ~/domains/api.spottedlezajsk.pl/public_nodejs && rm -rf dist node_modules app.js package.json package-lock.json && tar -xzf deploy.tar.gz && rm deploy.tar.gz && npm install --production && devil www restart api.spottedlezajsk.pl\" && rm deploy.tar.gz",
    "deploy:assets": "tar -czf assets.tar.gz -C public . && scp assets.tar.gz contdev@s56.mydevil.net:~/domains/api.spottedlezajsk.pl/public_nodejs/ && ssh contdev@s56.mydevil.net \"cd ~/domains/api.spottedlezajsk.pl/public_nodejs && rm -rf public && mkdir -p public && tar -xzf assets.tar.gz -C public && rm assets.tar.gz\" && rm assets.tar.gz"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.2.4",
    "express": "^5.2.1",
    "express-rate-limit": "^8.2.1",
    "express-validator": "^7.3.1",
    "helmet": "^8.1.0",
    "hpp": "^0.2.3",
    "multer": "^2.0.2",
    "nodemailer": "^8.0.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/hpp": "^0.2.6",
    "@types/multer": "^1.4.12",
    "@types/node": "^22.0.0",
    "@types/nodemailer": "^6.4.17",
    "nodemon": "^3.1.0",
    "rimraf": "^6.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  }
}
